import 'package:flutter/cupertino.dart';

class AppDimens {
  //main page padding
  static const pagePadding = EdgeInsets.fromLTRB(15, 0, 15, 0);

  //boarder radius
  static final ssBoarderRadius = BorderRadius.circular(5);
  static final sBoarderRadius = BorderRadius.circular(10);
  static final mBoarderRadius = BorderRadius.circular(15);
  static final lBoarderRadius = BorderRadius.circular(20);

//boarder margin
  static const ssMargin = EdgeInsets.all(4);
  static const smMargin = EdgeInsets.all(8);
  static const sMargin = EdgeInsets.all(10);

  static const mMargin = EdgeInsets.all(15);

  static const sRightMargin = EdgeInsets.only(right: 10);
  static const sLeftMargin = EdgeInsets.only(left: 10);
  static const mRightMargin = EdgeInsets.only(right: 20);
  static const mLeftMargin = EdgeInsets.only(left: 20);

//padding only
  static const lrtbPadding =
      EdgeInsets.only(left: 8, right: 8, top: 4, bottom: 4);
  static const topPadding = EdgeInsets.only(top: 10.0);
  static const leftPadding = EdgeInsets.only(left: 10.0);
  static const bottomPadding = EdgeInsets.only(bottom: 10.0);
  static const rightPadding = EdgeInsets.only(right: 10.0);

  //radius circular of container
  static const sradiusCircular = Radius.circular(5);
  static const mradiusCircular = Radius.circular(15);

//height
  static double sheight = 20.0;
  static double mheight = 30.0;
  static double mmheight = 35.0;
  static double lheight = 40.0;
  static double xlheight = 50.0;
  static double xxheight = 60.0;
}
